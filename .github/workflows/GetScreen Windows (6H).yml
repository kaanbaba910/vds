name: GetScreen Windows (6H)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=gattarx5522805832@gmail.com" >

      - name: Time Counter
        run: Start-Sleep  -Seconds 2147483
